[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
rootDirectory = "backend"
startTimeout = 600

[env]
PORT = "8000"
PYTHON_VERSION = "3.11"
