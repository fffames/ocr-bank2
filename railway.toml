[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
rootDirectory = "."
# Give app more time to start (Railway needs time to pull Docker image)
startTimeout = 600

[env]
PORT = "8000"
