[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
# Disable healthcheck - app needs more startup time than healthcheck allows
# healthcheckPath = "/health"
# healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
rootDirectory = "."
startTimeout = 1200

[env]
PORT = "8000"
