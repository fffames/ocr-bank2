[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile.minimal"

[deploy]
# Temporarily disable healthcheck to test deployment
# healthcheckPath = "/health"
# healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
rootDirectory = "."
startTimeout = 600

[env]
# Don't set PORT - let Railway assign it automatically
