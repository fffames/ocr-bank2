FROM python:3.11-slim

WORKDIR /app

# Install FastAPI and Uvicorn
RUN pip install fastapi uvicorn

# Copy the test app
COPY backend/test_app.py .

# Expose port
EXPOSE 8000

# Run the test app
CMD ["python", "-m", "uvicorn", "test_app:app", "--host", "0.0.0.0", "--port", "8000"]