[phases.setup]
nixPkgs = ["...", "tesseract", "libomp"]

[phases.build]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"

[variables]
PORT = "8000"
PYTHON_VERSION = "3.11"